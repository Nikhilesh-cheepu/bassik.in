import TeamClient from "./TeamClient";

export const metadata = {
  title: "Team — Ads & SEO | Bassik",
  robots: { index: false, follow: false },
};

export default function TeamPage() {
  return <TeamClient />;
}
