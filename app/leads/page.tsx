import LeadsManagerClient from "./LeadsManagerClient";

export const metadata = {
  title: "Leads Manager | Bassik",
  robots: { index: false, follow: false },
};

export default function LeadsPage() {
  return <LeadsManagerClient />;
}
